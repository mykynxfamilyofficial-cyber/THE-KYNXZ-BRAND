"use client";

/**
 * HeroClientFX — lightweight hero effects only.
 * No mouse parallax, no particle systems, no cursor aura, no floating clusters.
 */
export default function HeroClientFX() {
  return <>{/* All heavy effects removed for performance */}</>;
}
