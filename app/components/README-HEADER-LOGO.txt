TEMP NOTE: Only header logo issue investigated. Header uses next/image with theme state and toggles html data-theme. Public files logo-dark.png and logo-light.png exist. If logo not rendering, likely Next/Image cannot resolve theme state on first render; keep logic but ensure stable theme read is correct.

