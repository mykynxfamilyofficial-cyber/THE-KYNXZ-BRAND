/**
 * REMOVED: Not wired into pages yet. The about, contact, and mission pages
 * define their own local Section components via forwardRef.
 * Re-create if you want to refactor those pages to share this wrapper.
 */
export {}
