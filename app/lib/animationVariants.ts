/**
 * REMOVED: This shared file was created as a target for refactoring, but
 * no page currently imports from it. All pages define their own local
 * animation variant objects.
 *
 * To use this file, import the variants into each page and remove the
 * local definitions. Example:
 *   import { fadeUp, fadeIn, wordReveal } from "@/app/lib/animationVariants"
 */
export {}
